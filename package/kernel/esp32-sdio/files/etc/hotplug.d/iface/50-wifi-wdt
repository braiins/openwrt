. /lib/functions/bos-factory-default.sh

if [ "${ACTION}" = "ifdown" ] && [ "${INTERFACE}" = "${FACTORY_DEFAULT_STA_INTF_NAME}" ]; then
    logger -t "wifi-wdt" "Interface ${FACTORY_DEFAULT_ETH_INTF_NAME} goes down, trying to set it up again"

    wifi up
fi

