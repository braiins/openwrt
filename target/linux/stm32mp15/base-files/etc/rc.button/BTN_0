#!/bin/sh

[ "${ACTION}" = "pressed" ] || exit 0

echo "Pressed IP Report button" | logger -t rc.button

/usr/sbin/miner fault_light on
/usr/sbin/miner ip_report
/usr/sbin/miner fault_light off

return 0
